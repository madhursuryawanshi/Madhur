using system;
namespace
{
    public class P
    {
        protected interface limit
protected bool is isprime;
public prime(int limit)
        {
            this.limit = limit;
            isPrime = new bool limit + 1;
            initialize();
        }
    }
}
private void initialize()
{
    for (int i = 2; i <= limit; i++)
    {
        isPrime[i] = true;
    }
}